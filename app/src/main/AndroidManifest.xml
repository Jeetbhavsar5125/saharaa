<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission for Speech-to-Text -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Saharaa">

        <!-- 1️⃣ Splash Screen (Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Saharaa.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2️⃣ Accessibility Mode -->
        <activity
            android:name=".activities.AccessibilityModeActivity"
            android:exported="false" />

        <!-- 3️⃣ Language Selection -->
        <activity
            android:name=".activities.LanguageSelectionActivity"
            android:exported="false" />

        <!-- 4️⃣ Login / Register -->
        <activity
            android:name=".activities.LoginRegisterActivity"
            android:exported="false" />

        <!-- 5️⃣ Scanner -->
        <activity
            android:name=".activities.SmartScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- 6️⃣ Settings -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

        <!-- 7️⃣ Main Screen -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- 8️⃣ History -->
        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false" />


    </application>

</manifest>